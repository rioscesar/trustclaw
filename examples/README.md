# Examples

The first executable example is exposed through `pnpm demo`. It proposes a
simulated `gmail.delete_email` action and never connects to Gmail.

The demo uses the synthetic threshold shown in `config/policy.example.json`.
That value is a test fixture, not a recommended production policy.
